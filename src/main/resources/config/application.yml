# Disable the spring security default configuration from spring-boot-actuator
management.security.enabled: true
security.basic.enabled: false

# Disable Jolokia - An http/json bridge for remote JMX access
endpoints.jolokia.enabled: false

# security configuration (this key should be unique for your application, and kept secret)
jhipster.security.rememberme.key: 9a643507ff3724079e186ec884447a3875a61090

async:
    corePoolSize: 2
    maxPoolSize: 50
    queueCapacity: 10000

mail:
    host: relay.hrc.corp
    port: 25
    username:
    password:
    protocol: smtp
    tls: true
    auth: true
    from: VisiView@hill-rom.com
    to: leah.noaeill1@hill-rom.com,vinay.joshi@hill-rom.com,brett.quada@hill-rom.com,VisiView@hill-rom.com

authentication:
    xauth:
        secret: myXAuthSecret
        # Token is valid 30 minutes
        tokenValidityInSeconds: 1800

spring.batch.job.enabled: false
